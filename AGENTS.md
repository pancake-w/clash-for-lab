# Agent guidance

- Codex Remote networking: before changing proxy/TUN behavior or diagnosing repeated `Reconnecting... waiting for network` events, read `docs/codex-remote-tun.md`. Follow its verification order and preserve the rule that no subscription URL, controller secret, or node credential is committed.
